@model HMS.Areas.ViewModels.AccomodationTypesListingViewModel


@if (Model != null && Model.AccomodationTypes != null && Model.AccomodationTypes.Count() > 0)
{
    <b class="">@Model.AccomodationTypes.Count() result(s) found</b>
    <table class="table table-striped table-sm table-responsive-xl">
        <thead class="bg-secondary text-white">
        <tr>

            <th scope="col">Name</th>
            <th scope="col">Description</th>
            <th scope="col">Actions</th>
        </tr>
        </thead>
        <tbody>

        @foreach (var t in Model.AccomodationTypes)
        {
            <tr>

                <td>
                    @t.Name
                </td>
                <td>
                    @t.Description
                </td>
                <td>
                    <button type="button" data-href="@Url.Action("Action", "AccomodationTypes", new {Id = t.Id})" class="data-btn btn btn-primary btn-sm" data-toggle="modal" data-target="#actionModal">
                        <i class="fas fa-pen mr-1"></i>Edit
                    </button>
                    <button type="button" data-href="@Url.Action("Action", "AccomodationTypes", new {Id = t.Id, isDelete = true})" class="data-btn btn btn-danger btn-sm"  data-toggle="modal" data-target="#actionModal">
                        <i class="fas fa-trash-alt mr-1"></i>Delete
                    </button>

                </td>
            </tr>
        }

        </tbody>
    </table>
}
else
{
    <div class="alert alert-warning" role="alert">
        Sorry! no Record exist!
    </div>
}